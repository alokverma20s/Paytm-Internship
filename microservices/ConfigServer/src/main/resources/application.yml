server:
  port: 8085


spring:
  application:
    name: ConfigServer
  
  cloud:
    config:
      server:
        git:
          uri: https://github.com/alokverma20s/micro-service-config-server.git
          clone-on-start: true